---
featured: true
title:  直播網站實戰開發筆記 Part's 2 - 打造自己的直播播放器
date:   2022-05-27 01:01:01 +0800
thumbnail:  stream.webp
category: Frontend
tags:   ['直播','streaming']
description : 直播網站實戰開發筆記 Part's 2 - 打造自己的直播播放器
author : Mark Ku
slug: live-streaming-player
---
# 用 Vue 動手打造個人專屬的前端直播播放器的採用( hls.js )
## 前言
[上一篇](https://blog.markkulab.net/2021/07/20/streaming/)，談到用阿里雲來搭建直播伺服器，這篇我要來談前端直播放器，前端直播技術，最常見的有 HLS 、DASH 這兩種直播協定，本篇就主要會以這兩個進行探究

## 科技公司的分庭抗禮
早期的直播協議會讓人想到 Flash(FLV)協議，蘋果公司為了代替 Flash 與 RTMP與，推出了 HLS 直播協定，且為蘋果公司所私有，並在所有蘋果相關的產品中皆能直接使用。  

DASH 是 MPEG 公司為了和蘋果公司的 HLS 分庭抗禮，所延申的標準協議，比HLS 延遲更低，並將其開源。  

全球因有大量的 iOS 用戶，所以目前仍然有較多的網站傾向採用 HLS 的直播協定，但一些科技巨頭，反而是選擇了開源 DASH 協定。  

採用 HLS 公司有:  
foxsports、17LIVE、Twitter、球會體育、雷速體育  

採用 DASH 公司有:  
Netflix、Hulu、YouTube   

## 什麼是HLS
HTTP Live Streaming，縮寫為HLS，是由蘋果公司提出基於HTTP的串流媒體網路傳輸協定。是蘋果公司QuickTime X和iPhone軟體系統的一部分。它的工作原理是把整個流分成一個個小的基於HTTP的檔案來下載，每次只下載一些。當媒體流正在播放時，客戶端可以選擇從許多不同的備用源中以不同的速率下載同樣的資源，允許串流媒體對談適應不同的資料速率。在開始一個串流媒體對談時，客戶端會下載一個包含元資料的擴充 M3U (m3u8) 播放列表檔案，用於尋找可用的媒體流。 ( 維基 )

## 談到 HTTP Live Streaming (HLS) 的相容性問題
![](https://i.imgur.com/Nqhf4Uv.png)

先前提到 HTTP Live Streaming 是蘋果所提出，所以在預設瀏覽器 Safari 上，是可以直接播放的， 但 Chrome 等其他瀏覽器是不能播放的。

dailymotion 公司提供了開源套件 Hls.js 的解決跨瀏覽器相容性的問題。

## 專案現況 
那麼，由前面得知，我們要直接透 HTML VIDEO 播放串流是不可行的，基於用戶端相容性及 Cloud 支援度考量，本篇採用 HLS 協議進行開發。

## 動手作
### 1.安裝 hls js 
```
npm i hls.js --save
```

### 2. HLS JS 的使用方式

在引入 Hls.js 後，我們可以呼叫Hls.isSupported() 判斷瀏覽器是否支援 Hls，如果支援，我們可以，直接使用 video player 直接播放。
```
mounted() {
	let videoSrc = "https://storage.googleapis.com/shaka-demo-assets/angel-one-widevine-hls/hls.m3u8"
	
	const video = document.getElementById('live-player')
	this.player = video

	if (Hls.isSupported()) {
	const hls = new Hls()
	hls.loadSource(videoSrc)
	hls.attachMedia(video)
	} else {
	video.src = videoSrc
	}
}
```

## 3.依據上述兩點，你己經可以播放 HLS 格式直播串流影片但是如果有留意，使用者如果離開此頁面時，在開發者模式中，頁面還是不斷的發出大請求，最後導致記憶體洩漏。

## 4.那麼，為了避免上述問題，我們必須在元件銷毀前，將 hls 停用及銷毀
```
  beforeDestroy() {     	  
      if (Object.keys(this.hls).length > 0) {
        this.hls.stopLoad()
        this.hls.destroy()
        this.hls = null
      }
  },
```

## 5.但是需求往往沒有這麼簡單，使用者會希望調整音量，切換影片解析度、頁面捲動時擁有浮動功能、彈慕、全屏發彈慕等功能。

## 6. 實作音量調整功能
如果是設計手機版的播放器是不能透過 video 去調整音量，而是要透過手機的硬體調整音量，則桌機版可以透過一些 slider 的套件去控制音量。

```
handleSound(e) {
this.player.volume = e / 100
},
```

## 7. 頁面載入自動播放限制
許多瀏覽器為了提昇使用者體驗而限制了 autoplay 的條件，如果你要實作 Auto Player。   
1. 必須要是靜音。
`<video controls muted>`
2. 使用者跟網站有互動後，點擊後才能夠自動播放。

## 8. 浮動播放器
### 8.1 實作理很簡單，在播放器播放時，去監聽主要的捲軸，當捲軸的捲動超過播放器的高度時，將播放器的 position 改成 fixed

```
&.floating-video {
    position: fixed;
    right: 0;
    bottom: 240px;
    z-index: 10;
    border-radius: 16px 16px ;
    width: 420px !important;
    height: 282px !important;
    animation: popin 0.5s ease-in-out forwards;
    font-size: 16px

    @keyframes popin {
      to {
        bottom: 240px;
      }
    }
```

### 8.2 監聽捲動事件

```
 document.addEventListener('scroll', function(e) {
 // 當播放器不是暫停，捲動才作用
if (!that.player.paused && document.scrollTop > videoHeight) {
	// 當不是浮動的狀態
   if (!that.isFloatingVideo) {
       that.isFloatingVideo = true  // 用來控制 class 是不是浮動的樣式
   } else {
     that.isFloatingVideo = false
     }
	 }
 })
```

## 9. 客製化播放器全屏
### 9.1 客製化播放器上有很多客製的功能選項，全屏如果PC版，你可以很輕易透過瀏覽器 api ，可以將客製化播放器全屏

```
handleFull() {
       const ele = this.player
       if (ele.requestFullscreen) {
         ele.requestFullscreen()
       } else if (ele.mozRequestFullScreen) {
         ele.mozRequestFullScreen()
       } else if (ele.webkitRequestFullScreen) {
         ele.webkitRequestFullScreen()
       } else if (ele.webkitEnterFullscreen) {
         ele.webkitEnterFullscreen()
       } else if (ele.webkitRequestFullscreen) {
         ele.webkitRequestFullscreen()
       } else if (ele.msRequestFullscreen) {
         ele.msRequestFullscreen()
       }
},
```

### 9.2 但是手機版就不行了，你得透過 absolute 去蓋一層 div 在頁面。
參考 [Bilibili]的客製化播放器(https://www.bilibili.com/) 
![](https://i.imgur.com/Cqp9Il0.png)  
![](https://i.imgur.com/sMZBXGK.png)  

## 相關程式碼
```
<template>

  <div v-if="hasStreaming">
    <div ref="videoPlayerBox" class="live-player-wrapper" :style="videoClassObject">
      <div v-if="loading" class="loading-layer">
        <img
          :src="require(`@/assets/images/video-player/loading.svg`)"
          alt="loading"
        >
      </div>
      <div v-if="error || isLiveClose" class="empty-room-wrapper">
        <p class="empty-title">主播暫不在播哦</p>
        <p class="empty-tip">關注主播可收到開播提醒</p>
        <div v-if="recommendList.data" class="empty-recommend">
          <p class="recommend-title">為你推薦</p>
          <ul class="recommend-list">
            <li v-for="room in recommendList.data" :key="room.id" class="recommend-item">
              <router-link :to="{path: '/liveroom', query: {id: room.id}}" target="_blank" />
            </li>
          </ul>
        </div>
      </div>

      <video
        id="live-player"
        ref="myPlayer"
        class="video-layer"
        preload="none"
        :style="videoClassObject"
        :poster="poster"
        @click="togglePlay"
        @playing="onPlaying"
        @pause="isPlaying = false"
        @seeking="loading = true"
        @seeked="loading = false"
        @error="videoError"
        @loadeddata="videoCanplay"
        @canplay="canplay"
      />
      <!-- 底圖 -->

      <img v-if="!isPlaying || isFloatingVideo" :src="poster ?poster: require(`@/assets/images/video-player/video-player-bg.png`)" class="poster" alt="" :style="videoClassObject">

      <div
        v-if="!disBaberrage"
        ref="controlBar"
        class="control-bar"
        :class="{active: activePlayer}"
      >
        <div class="progress-bar">
          <Slider id="slider" v-model="sliderV" :tip-formatter="tipFormatter" @change="handleTimeSlider" @afterChange="sliderEnd" />
        </div>

        <div class="main-layer">
          <div class="left-layer">
            <img v-if="loading" :src="require(`@/assets/images/video-player/loading.svg`)" alt="loading" style="width: 25px; height: 25px;">
            <span v-else class="play-btn" :class="{paused: isPlaying}" @click.stop="togglePlay" />
            <div v-if="isVideoOnDemand" class="duration-layer">
              <span class="current">{{ formatTime(currentTime) }}</span>&nbsp;/&nbsp;<span>{{ formatTime(player.duration || 0) }}</span>
            </div>
            <div v-else class="live-layer">Live</div>
          </div>
          <div class="right-layer">
            <div class="action-layer" @click="openResolution = true">
              {{ $lang('Common.ResolutionType_'+currentStream.type) }}
              <ul v-if="openResolution" class="resolution-select open-layer" :style="{top: ( -40 * streamings.length) +'px'}">
                <li v-for="(item) in streamings" :key="item.type" :class="{active: item.type === currentStream.type}" @click="switchResolution(item)">{{ $lang('Common.ResolutionType_'+ item.type) }} </li>
              </ul>

            </div>

            <div v-if="isVideoOnDemand" class="action-layer seek-layer" @mouseleave="openSeek = false">
              <span @click="openSeek = !openSeek">倍速</span>
              <ul v-if="openSeek" class="seek-select open-layer">
                <li v-for="(item, index) in seekArr" :key="index" :class="{active: seekIndex === index}" @click="seekClick(item, index)">{{ item }}x</li>
              </ul>
            </div>

            <div class="sound-btn" :class="{active: soundActive, muted: isMuted}" @click="soundClick" @mouseleave="openSound = false">
              <div v-if="openSound" class="open-layer" @click.stop>
                <div class="sound-control">
                  <Slider v-model="soundV" vertical @change="handleSound" />
                </div>
              </div>
            </div>
            <div class="full-btn" :class="{active: isFullScreen}" @click="handleFull" />
          </div>
        </div>
      </div>

    </div>
  </div>
  <div v-else>
    <div class="flex justify-center align-center no-video" :style="videoClassObject">
      暫無視頻
    </div>
  </div>
</template>

<script>
import { Slider } from 'ant-design-vue'
import { ResolutionType } from '@/config/index'
import { liveRoomPage } from '@/api/liveRoom'
const Hls = require('hls.js')
export default {
  components: { Slider },
  props: {
    width: {
      type: [String, Number],
      default: 850
    },
    height: {
      type: [String, Number],
      default: 480
    },
    hasStreaming: {
      type: Boolean,
      default: true
    },
    streamings: {
      type: Array,
      default: () => { [] }
      // https://live.comsaw.com/live/sd-1-3580751.m3u8?txSecret=c6fffadeff432578a6c2583fd9e7fba0&txTime=615347B3  // 點播
      //   default: 'http://pull.comsaw.com/live/26.m3u8?auth_key=1632463027-0-0-df6f2eba06bb7e060341bd43ac04bc72' // 直播
    },
    poster: {
      type: String,
      default: ''
    },
    disBaberrage: {
      type: Boolean,
      default: false
    },
    isLiveClose: {
      type: Boolean,
      default: false
    },
    isVideoOnDemand: { // 是否點播
      type: Boolean,
      default: false
    },
    enableFloating: { // 是否點播
      type: Boolean,
      default: true
    },
    autoPlay: { // 自動播放
      type: Boolean,
      default: false,
      required: false
    }
  },
  data() {
    return {
      videoLoaded: false,
      sliderV: 0,
      seekArr: [0.5, 1, 1.25, 1.5, 2],
      myPlayer: {},
      isPlaying: false,
      currentTime: '0',
      duration: '0',
      player: {},
      soundActive: false,
      isFullScreen: false,
      showControl: false,
      seekIndex: 1,
      openSeek: true,
      openSound: false,
      soundV: 50,
      isMuted: false,
      fullTimer: null,
      loading: true,
      barrageIsShow: true,
      barrageLoop: false,
      barrageList: [],
      activePlayer: false,
      hideBaberrage: false,
      hoverLanePause: true,
      error: false,
      recommendList: {},
      hls: {},
      isFloatingVideo: false,
      openResolution: false,
      currentResolution: ResolutionType.ChineseStandard
    }
  },
  computed: {
    currentStream() {
      if (this.streamings) {
        const currentStream = this.streamings.find(x => x.type === this.currentResolution)

        if (currentStream) {
          return currentStream
        } else if (this.streamings.length > 0) {
          return this.streamings[0]
        }
      }

      return { url: '', type: 0 }
    },
    videoClassObject() {
      if (this.isFullScreen) {
        return { height: '100%', width: '100%' }
      }

      if (this.isFloatingVideo) {
        return { height: '200px', width: '400px' }
      }

      return { height: this.height + 'px' || '100%', width: this.width + 'px' || '100%' }
    }
  },
  watch: {},
  mounted() {
    if (!this.hasStreaming) { return }

    this.isPlaying = this.autoPlay
    this.loading = this.autoPlay

    if (this.autoPlay) {
      this.togglePlay()
    }
  },
  beforeDestroy() {
    this.destroyVideo()
  },
  methods: {
    destroyVideo() {
      if (this.$refs.myPlayer) {
        this.$refs.myPlayer.pause()
        this.$refs.myPlayer && this.$refs.myPlayer.removeAttribute('src')
      }
      if (Object.keys(this.hls).length > 0) {
        this.hls.stopLoad()
        this.hls.destroy()
        this.hls = null
      }
    },
    switchResolution(item) {
      this.destroyVideo()
      this.isVideoLoaded = false
      this.currentResolution = item.type
      this.togglePlay()
    },
    togglePlay() {
      if (!this.hasStreaming) { return }

      const video = document.getElementById('live-player')
      this.player = video

      if (!this.isVideoLoaded) {
        const videoSrc = this.currentStream.url
        const isMp4 = this.currentStream.url.indexOf('mp4') > -1

        if (Hls.isSupported() && !isMp4) {
          const hlss = new Hls()
          hlss.loadSource(videoSrc)
          hlss.attachMedia(video)
          this.hls = hlss
          this.isVideoLoaded = true
        } else {
          video.src = videoSrc
          this.isVideoLoaded = true
        }

        // 靜音
        // // video.muted = true
        // // this.isMuted = true
        video.load()

        video.oncanplay = function() {
          console.log('準備就緒')
        }

        if (this.isVideoOnDemand) {
          this.player.addEventListener('timeupdate', e => {
            this.updateCurrentTime()
          },
          false
          )
        }

        const that = this
        this.$refs.videoPlayerBox.addEventListener('dblclick', function() {
          that.handleFull()
        })

        document.addEventListener('fullscreenchange', function(e) {
          if (!document.fullscreenElement) {
            that.$refs.videoPlayerBox.removeEventListener('mousemove', that.moveFn)
            that.isFullScreen && that.handleFull()
          }
        })
        document.addEventListener('keydown', function(e) {
          if (e.code === 'Space') e.preventDefault()
        })
      }

      this.$refs.myPlayer.paused ? this.$refs.myPlayer.play() : this.$refs.myPlayer.pause()

      this.isPlaying = !this.$refs.myPlayer.paused
    },
    updateCurrentTime() {
      this.sliderV = this.player.currentTime / this.player.duration * 100
      this.currentTime = this.player.currentTime
    },
    handleTimeSlider(e) {
      this.player.pause()
      this.player.currentTime = (this.player.duration * e) / 100
    },
    tipFormatter(v) {
      if (!this.player) return
      const s = (this.player.duration * v) / 100
      return this.formatTime(s)
    },
    switchDefinition() {
      alert('暫無實作')
    },
    canplay() {
    },
    onPlaying() {
      this.isPlaying = true
      this.loading = false
      this.error = false
      this.activePlayer = true

      if (this.enableFloating) {
        const that = this
        const videoHeight = that.$refs.videoPlayerBox.offsetHeight
        window.addEventListener('scroll', function(e) {
          if (!that.player.paused && window.scrollY > videoHeight) {
            if (!that.isFloatingVideo) {
              that.$refs.videoPlayerBox.classList.add('popout-video--popout')

              that.$refs.videoPlayerBox.style.bottom = `110px`
              that.isFloatingVideo = true
            }
          } else {
            that.$refs.videoPlayerBox.classList.remove('popout-video--popout')
            that.$refs.videoPlayerBox.style.bottom = `0`
            that.isFloatingVideo = false
          }
        })
      }
    },
    sliderEnd() {
      if (this.player.paused) {
        this.player.play()
      }
    },

    formatTime(time) {
      let minutes = Math.floor(time % 3600 / 60)
      if (minutes < 10) {
        minutes = '0' + minutes
      }
      // 秒
      let seconds = parseInt(time % 60)
      if (seconds < 10) {
        seconds = '0' + seconds
      }
      return minutes + ':' + seconds
    },
    handleFull() {
      if (this.isFullScreen) {
        // this.height = ''
        // this.width = ''

        this.exitFullscreen()
        this.isFullScreen = false
        clearTimeout(this.fullTimer)
        this.showControl = false
      } else {
        this.inFullScreen(this.$refs.videoPlayerBox)
        this.isFullScreen = true
        this.$refs.videoPlayerBox.addEventListener('mousemove', this.moveFn)
      }
    },
    handleSound(e) {
      this.player.volume = e / 100
      this.isMuted = !e
    },
    soundClick() {
      if (this.isMuted) {
        this.player.volume = 0.5
        this.soundV = 50
        this.isMuted = false
        this.$refs.myPlayer.muted = false
        return
      }
      if (this.openSound) {
        this.isMuted = true
        this.player.volume = 0
        this.soundV = 0
      } else {
        this.openSound = true
      }
    },
    inFullScreen(ele) {
      if (ele.requestFullscreen) {
        ele.requestFullscreen()
      } else if (ele.mozRequestFullScreen) {
        ele.mozRequestFullScreen()
      } else if (ele.webkitRequestFullScreen) {
        ele.webkitRequestFullScreen()
      }
    },
    exitFullscreen() {
      const de = document
      if (de.exitFullscreen) {
        de.exitFullscreen()
      } else if (de.mozCancelFullScreen) {
        de.mozCancelFullScreen()
      } else if (de.webkitCancelFullScreen) {
        de.webkitCancelFullScreen()
      }
    },
    seekClick(rate, index) {
      this.player.playbackRate = rate
      this.seekIndex = index
    },
    moveFn() {
      if (this.fullTimer) {
        clearTimeout(this.fullTimer)
      }
      this.showControl = true
      this.fullTimer = setTimeout(() => {
        this.showControl = false
      }, 3000)
    },
    videoError() {
      if (!this.disBaberrage) return
      this.loading = false
      this.error = true
      this.player.pause()
      this.player.removeAttribute('src')
      this.player.load()
      liveRoomPage({
        isOnlyTop: true,
        pageIndex: 1,
        pageSize: 3
      }).then(res => {
        this.recommendList = res
      })
    },
    videoCanplay() {

    }
  }
}
</script>

<style lang='scss' scoped rel='stylesheet/scss'>
.sk-chase {
  position: relative;
  width: 50px;
  height: 50px;
  animation: sk-chase 2.5s infinite linear both;
}

.sk-chase-dot {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  animation: sk-chase-dot 2s infinite ease-in-out both;
}

.sk-chase-dot::before {
  display: block;
  border-radius: 100%;
  width: 25%;
  height: 25%;
  background-color: #237FFF;
  content: '';
  animation: sk-chase-dot-before 2s infinite ease-in-out both;
}

.sk-chase-dot:nth-child(1) { animation-delay: -1.1s; }
.sk-chase-dot:nth-child(2) { animation-delay: -1s; }
.sk-chase-dot:nth-child(3) { animation-delay: -0.9s; }
.sk-chase-dot:nth-child(4) { animation-delay: -0.8s; }
.sk-chase-dot:nth-child(5) { animation-delay: -0.7s; }
.sk-chase-dot:nth-child(6) { animation-delay: -0.6s; }
.sk-chase-dot:nth-child(1)::before { animation-delay: -1.1s; }
.sk-chase-dot:nth-child(2)::before { animation-delay: -1s; }
.sk-chase-dot:nth-child(3)::before { animation-delay: -0.9s; }
.sk-chase-dot:nth-child(4)::before { animation-delay: -0.8s; }
.sk-chase-dot:nth-child(5)::before { animation-delay: -0.7s; }
.sk-chase-dot:nth-child(6)::before { animation-delay: -0.6s; }

@keyframes sk-chase {
  100% { transform: rotate(360deg); }
}

@keyframes sk-chase-dot {
  80%,
  100% { transform: rotate(360deg); }
}

@keyframes sk-chase-dot-before {
  50% {
    transform: scale(0.4);
  }

  100%,
  0% {
    transform: scale(1);
  }
}

.live-player-wrapper {
  position: relative;
  overflow: hidden;
  width: fit-content;
  width: 100%;
  height: 100%;

  .baberrage-wrapper {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 300px;
  }

  .loading-layer,
  .video-layer {
    position: absolute;
    top: 50%;
    left: 50%;
    z-index: 99;
    text-align: center;
    color: #FFF;
    transform: translate(-50%, -50%);

    ::v-deep.ant-spin-dot-item {
      background-color: #227FFF;
      opacity: 0.5;
    }

    &.err {
      top: 0;
      left: 0;
      padding-top: 200px;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.3);
      transform: translate(0, 0);

      img {
        width: 40px;
        height: 40px;
      }

      p {
        margin-top: 20px;
      }
    }
  }

  &.full-screen {
    .control-bar {
      opacity: 0;
      transform: translateY(100%);
    }
  }

  &.show-control {
    .control-bar {
      opacity: 1;
      transform: translateY(0);
    }
  }

  video {
    &::-webkit-media-controls {
      display: none !important;
    }

    &.full {
      width: 100%;
      height: 100%;
    }
  }

  .control-bar {
    position: absolute;
    bottom: 0;
    left: 0;
    z-index: 9999;
    width: 100%;
    height: 55px;
    background: linear-gradient(180deg, rgba(0, 0, 0, 0) 0%, rgba(0, 0, 0, 0.85) 100%);
    transition: all 0.2s cubic-bezier(1, 0.5, 0.8, 1);

    &.active {
      opacity: 0;
      transform: translateY(100%);
    }

    &.full {
      position: fixed;
      bottom: 0;
      left: 0;
      z-index: 214748364999;
      width: 100%;
    }

    .main-layer {
      display: flex;
      justify-content: space-between;
      padding: 13px 11px;
      width: 100%;
      height: 51px;
      box-sizing: border-box;

      .left-layer {
        display: flex;
        align-items: center;
        color: rgba(255, 255, 255, 0.5);

        .duration-layer,
        .live-layer {
          margin-top: 2px;
          margin-left: 23px;

          .current {
            color: #FFF;
          }
        }
      }

      .right-layer {
        display: flex;
        align-items: center;

        .action-layer {
          position: relative;
          display: inline-block;
          margin-right: 15px;
          border-radius: 12px;
          width: 72px;
          height: 24px;
          font-size: 14px;
          text-align: center;
          color: #FFF;
          background-color: rgba(48, 48, 48, 0.6);
          line-height: 24px;
          cursor: pointer;

          &:hover {
            color: #FFF;
            background-color: #237FFF;
          }

          .seek-select {
            position: absolute;
            top: -210px;
            left: 50%;
            border-radius: 4px;
            padding-bottom: 22px;
            width: 84px;
            height: 210px;
            transform: translateX(-50%);

            li {
              width: 100%;
              height: 20%;
              text-align: center;
              background-color: rgba(37, 39, 47, 0.9);
              line-height: 38px;
              cursor: pointer;

              &.active {
                color: #237FFF;
              }
            }
          }

          .resolution-select {
            position: absolute;
            left: 50%;
            border-radius: 4px;
            padding-bottom: 22px;
            width: 76px;
            height: 210px;
            transform: translateX(-50%);

            li {
              width: 100%;
              height: 20%;
              text-align: center;
              background-color: rgba(37, 39, 47, 0.9);
              line-height: 38px;
              cursor: pointer;

              &.active {
                color: #237FFF;
              }
            }
          }
        }

        .sound-btn {
          position: relative;
          margin-right: 15px;
          background-image: url("~assets/images/video-player/sound-n.png");

          &.muted {
            background-image: url("~assets/images/video-player/mute-n.png");
          }

          .open-layer {
            position: absolute;
            top: -137px;
            left: 50%;
            padding-bottom: 21px;
            width: 40px;
            height: 137px;
            transform: translateX(-50%);

            .sound-control {
              display: flex;
              justify-content: center;
              align-items: center;
              border-radius: 4px;
              padding: 8px 0;
              width: 100%;
              height: 100%;
              background-color: rgba(37, 39, 47, 0.9);
              box-sizing: border-box;
            }
          }

          &:hover {
            background-image: url("~assets/images/video-player/sound-s.png");

            &.muted {
              background-image: url("~assets/images/video-player/mute-s.png");
            }
          }
        }

        .full-btn {
          background-image: url("~assets/images/video-player/full-n.png");

          &:hover {
            background-image: url("~assets/images/video-player/full-s.png");
            animation: icon_scale 0.4s ease both;
          }
        }
      }
    }

    .play-btn {
      display: inline-block;
      width: 24px;
      height: 24px;
      background-size: 24px 24px;
      background-image: url("~assets/images/video-player/play-n.png");
      cursor: pointer;

      &.paused {
        background-image: url("~assets/images/video-player/paused.png");

        &:hover {
          background-image: url("~assets/images/video-player/paused-s.png");
        }
      }

      &:hover {
        background-image: url("~assets/images/video-player/play-s.png");
      }
    }

    .progress-bar {
      ::v-deep.ant-slider {
        margin: 0;
        height: 8px;

        .ant-slider-track {
          background-color: #237FFF;
        }

        .ant-slider-rail {
          background-color: rgba(255, 255, 255, 0.3);
        }

        .ant-slider-handle {
          margin-top: -3px;
          width: 15px;
          height: 15px;
        }
      }
    }
  }

  .empty-room-wrapper {
    padding-top: 83px;
    width: 100%;
    height: 100%;
    text-align: center;
    color: #FFF;
    background: #2C3A55;
    box-sizing: border-box;

    .empty-title {
      font-size: 28px;
      line-height: 40px;
    }
  }

  .empty-tip {
    margin-top: 7px;
    font-size: 14px;
    opacity: 0.5;
    line-height: 20px;
  }

  .empty-recommend {
    margin: 44px 90px 0 90px;
    text-align: left;

    .recommend-title {
      font-size: 14px;
      opacity: 0.5;
      line-height: 20px;
    }

    .recommend-list {
      margin-top: 16px;
      width: 720px;
      text-align: left;

      .recommend-item {
        display: inline-block;
        margin-right: 19px;

        // &:nth-child(3n { & + 3) {
        //   margin-right: 0;
        // }
      }
    }
  }
}

.live-player-wrapper:hover .control-bar,
.live-player-wrapper.paused .control-bar {
  opacity: 1 !important;
  transform: translateY(0) !important;
}

.live-player-wrapper .control-bar .main-layer .right-layer .action-layer .open-layer,
.live-player-wrapper .control-bar .main-layer .right-layer .sound-btn .open-layer {
  display: none;
}

.live-player-wrapper .control-bar .main-layer .right-layer .action-layer:hover .open-layer,
.live-player-wrapper .control-bar .main-layer .right-layer .sound-btn:hover .open-layer {
  display: block;
}

.live-player-wrapper .control-bar .main-layer .right-layer .sound-btn,
.live-player-wrapper .control-bar .main-layer .right-layer .full-btn {
  display: inline-block;
  width: 24px;
  height: 24px;
  background-size: 24px 24px;
  cursor: pointer;
}

.popout-video {
  position: relative;
  width: 100%;

  &--popout {
    position: fixed;
    right: 0;    
    z-index: 10;
    max-width: 100%;
    animation: popin 0.5s ease-in-out forwards;

    @keyframes popin {
      to {
        bottom: 110px;
      }
    }

    &:hover {
      .popout-video__controls {
        opacity: 1;
      }
    }
  }
}

.no-video {
  color: white;
  background-color: $text-black;
}

</style>
```



